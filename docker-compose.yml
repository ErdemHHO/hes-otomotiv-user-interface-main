version: '3'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - 3000:3000

    command: npm run start

    environment:
      - NODE_OPTIONS=--dns-result-order=ipv4first

    dns:
      - 8.8.8.8
      - 1.1.1.1